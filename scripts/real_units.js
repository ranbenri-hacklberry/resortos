/** Fallback seed only — live inventory comes from public.resort_units. */
export const DEFAULT_RESORT_UNITS = [
  { id: 'k826', name: 'קאסה נובה · Aura', sort_order: 0, unit_type: 'suite', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k827', name: 'קאסה נובה · Bloom', sort_order: 1, unit_type: 'suite', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'lab-kinneret', name: 'בקתת כנרת', sort_order: 2, unit_type: 'cabin', max_occupancy: 4, base_price_agorot: 85000 },
  { id: 'hill-1', name: 'צימר בגבעה 1', sort_order: 3, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'hill-2', name: 'צימר בגבעה 2', sort_order: 4, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'hill-3', name: 'צימר בגבעה 3', sort_order: 5, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'hill-4', name: 'צימר בגבעה 4', sort_order: 6, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'dome-blue', name: 'כיפת שמיים כחול', sort_order: 7, unit_type: 'dome', max_occupancy: 3, base_price_agorot: 120000 },
  { id: 'dome-red', name: 'כיפת שמיים אדום', sort_order: 8, unit_type: 'dome', max_occupancy: 3, base_price_agorot: 120000 },
  { id: 'dome-green', name: 'כיפת שמיים ירוק', sort_order: 9, unit_type: 'dome', max_occupancy: 3, base_price_agorot: 120000 },
  { id: 'mialis-villa', name: 'וילה מיאליס ריזורט', sort_order: 10, unit_type: 'villa', max_occupancy: 23, base_price_agorot: 250000 },
  { id: 'suite-1', name: 'סוויטה 1 (הירוקה)', sort_order: 11, unit_type: 'suite', max_occupancy: 5, base_price_agorot: 150000 },
  { id: 'suite-2', name: 'סוויטה 2 (הורודה)', sort_order: 12, unit_type: 'suite', max_occupancy: 5, base_price_agorot: 150000 },
  { id: 'k671', name: 'בתי נורית 1', sort_order: 13, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k673', name: 'בתי נורית 2', sort_order: 14, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k674', name: 'בתי נורית 3', sort_order: 15, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k675', name: 'בתי נורית 4', sort_order: 16, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k676', name: 'בתי נורית 5', sort_order: 17, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k677', name: 'בתי נורית 6', sort_order: 18, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k678', name: 'בתי נורית 7', sort_order: 19, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k679', name: 'בתי נורית 8', sort_order: 20, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k808', name: "טאג' מאהל · בקתה 1", sort_order: 21, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k809', name: "טאג' מאהל · בקתה 2", sort_order: 22, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k810', name: "טאג' מאהל · בקתה 3", sort_order: 23, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k811', name: "טאג' מאהל · בקתה 4", sort_order: 24, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k680', name: 'מול הנוף · בקתה 1', sort_order: 25, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k681', name: 'מול הנוף · בקתה 2', sort_order: 26, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k682', name: 'מול הנוף · בקתה 3', sort_order: 27, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k683', name: 'מול הנוף · בקתה 4', sort_order: 28, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k684', name: 'מול הנוף · בקתה 5', sort_order: 29, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k685', name: 'נופים בלבן · בקתה 1', sort_order: 30, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k686', name: 'נופים בלבן · בקתה 2', sort_order: 31, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k687', name: 'טוסקנה · פירנצה 1', sort_order: 32, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k688', name: 'טוסקנה · פירנצה 2', sort_order: 33, unit_type: 'cabin', max_occupancy: 7, base_price_agorot: 85000 },
  { id: 'k689', name: 'טוסקנה · שאטו', sort_order: 34, unit_type: 'cabin', max_occupancy: 10, base_price_agorot: 85000 },
  { id: 'k690', name: 'חצר מוסיקלית · חליל', sort_order: 35, unit_type: 'cabin', max_occupancy: 4, base_price_agorot: 85000 },
  { id: 'k691', name: 'חצר מוסיקלית · מיתר', sort_order: 36, unit_type: 'cabin', max_occupancy: 4, base_price_agorot: 85000 },
  { id: 'k692', name: 'חצר מוסיקלית · פעמון', sort_order: 37, unit_type: 'cabin', max_occupancy: 4, base_price_agorot: 85000 },
  { id: 'k693', name: 'בקתות מאיה · בקתה 1', sort_order: 38, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k694', name: 'בקתות מאיה · בקתה 2', sort_order: 39, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 },
  { id: 'k695', name: 'בקתות מאיה · בקתה 3', sort_order: 40, unit_type: 'cabin', max_occupancy: 5, base_price_agorot: 85000 }
];

/** @deprecated use DEFAULT_RESORT_UNITS — kept so existing imports keep working */
export const CANONICAL_UNITS = DEFAULT_RESORT_UNITS;

const NAME_TO_ID = Object.fromEntries(
  DEFAULT_RESORT_UNITS.flatMap((unit) => [
    [unit.name, unit.id],
    [unit.id, unit.id]
  ])
);
NAME_TO_ID['מיאליס וילה'] = 'mialis-villa';
NAME_TO_ID['וילה מיאליס'] = 'mialis-villa';
NAME_TO_ID['בקתת כנרת'] = 'lab-kinneret';
NAME_TO_ID['סוויטה 1'] = 'suite-1';
NAME_TO_ID['סוויטה 2'] = 'suite-2';
NAME_TO_ID['סוויטה מיאליס ירוקה'] = 'suite-1';
NAME_TO_ID['סוויטה מיאליס ורודה'] = 'suite-2';
NAME_TO_ID['בתי נורית · בקתה 1'] = 'k671';
NAME_TO_ID['בתי נורית · בקתה 2'] = 'k673';
NAME_TO_ID['בתי נורית · בקתה 3'] = 'k674';
NAME_TO_ID['בתי נורית · בקתה 4'] = 'k675';
NAME_TO_ID['בתי נורית · בקתה 5'] = 'k676';
NAME_TO_ID['בתי נורית · בקתה 6'] = 'k677';
NAME_TO_ID['בתי נורית · בקתה 7'] = 'k678';
NAME_TO_ID['בתי נורית · בקתה 8'] = 'k679';

export const FORCE_DOCUMENT_NAMES = new Set([
  'mialis-villa', 'suite-1', 'suite-2',
  'k671', 'k673', 'k674', 'k675', 'k676', 'k677', 'k678', 'k679'
]);
NAME_TO_ID['סייסטה · רומנטית 1'] = 'k620';
NAME_TO_ID['סייסטה · רומנטית 2'] = 'k621';
NAME_TO_ID['סייסטה · בת הים 1'] = 'k622';
NAME_TO_ID['סייסטה · בת הים 2'] = 'k623';

export const RETIRED_UNIT_IDS = new Set([
  'u1', 'u2', 'u3', 'u4',
  'k618', 'k619', 'k620', 'k621', 'k622', 'k623'
]);

export function isPhantomUnitId(id) {
  return String(id || '').startsWith('u_t_');
}

export function isRetiredUnitId(id) {
  return RETIRED_UNIT_IDS.has(String(id || ''));
}

export function isInventoryUnit(unit) {
  if (!unit?.id || unit.deleted_at || unit.is_active === false) return false;
  if (isPhantomUnitId(unit.id) || isRetiredUnitId(unit.id)) return false;
  return true;
}

export function inventoryUnits(units) {
  const list = (units || []).filter(isInventoryUnit);
  return list.sort((a, b) => {
    const sa = Number.isFinite(Number(a.sort_order)) ? Number(a.sort_order) : 100;
    const sb = Number.isFinite(Number(b.sort_order)) ? Number(b.sort_order) : 100;
    if (sa !== sb) return sa - sb;
    return String(a.name || '').localeCompare(String(b.name || ''), 'he');
  });
}

export function normalizeAllowedUnitIds(value) {
  if (!Array.isArray(value)) return [];
  return [...new Set(value.map((id) => String(id || '').trim()).filter(Boolean))];
}

export function canSeeAllUnits(allowedUnitIds) {
  return normalizeAllowedUnitIds(allowedUnitIds).length === 0;
}

export function visibleInventory(units, allowedUnitIds) {
  const list = inventoryUnits(units);
  const allowed = normalizeAllowedUnitIds(allowedUnitIds);
  if (!allowed.length) return list;
  const allowedSet = new Set(allowed);
  const filtered = list.filter((unit) => allowedSet.has(unit.id));
  // Never blank the occupancy board because of a stale/mismatched ACL list.
  return filtered.length ? filtered : list;
}

/** Always have rows to paint — live Dexie first, then the canonical seed. */
export function calendarInventory(units, allowedUnitIds, tenantId) {
  const live = visibleInventory(units, allowedUnitIds);
  if (live.length) return live;
  return inventoryUnits(
    DEFAULT_RESORT_UNITS.map((unit) => ({
      ...unit,
      tenant_id: tenantId,
      is_active: true
    }))
  );
}

export function unitAccessGroup(unitId) {
  const id = String(unitId || '');
  if (id === 'lab-kinneret') return 'מעבדה';
  if (id === 'mialis-villa' || id.startsWith('suite-')) return 'מיאליס';
  if (id.startsWith('hill-')) return 'צימר בגבעה';
  if (id.startsWith('dome-')) return 'כיפת שמיים';
  if (/^k67[1-9]$/.test(id)) return 'בתי נורית · רמות';
  if (/^k80[8-9]$/.test(id) || id === 'k810' || id === 'k811') return "טאג' מאהל · רמות";
  if (/^k68[0-4]$/.test(id)) return 'מול הנוף · רמות';
  if (id === 'k685' || id === 'k686') return 'נופים בלבן · רמות';
  if (/^k68[7-9]$/.test(id)) return 'בקתות טוסקנה · רמות';
  if (/^k69[0-2]$/.test(id)) return 'החצר המוסיקלית · רמות';
  if (/^k69[3-5]$/.test(id)) return 'בקתות מאיה · רמות';
  if (id === 'k618' || id === 'k619' || /^k62[0-3]$/.test(id)) return 'סייסטה · רמות';
  if (id === 'k826' || id === 'k827') return 'קאסה נובה · נוב';
  return 'יחידות נוספות';
}

export function groupedInventoryUnits(units) {
  const groups = [];
  const index = new Map();
  for (const unit of inventoryUnits(units)) {
    const title = unitAccessGroup(unit.id);
    if (!index.has(title)) {
      const group = { title, units: [] };
      index.set(title, group);
      groups.push(group);
    }
    index.get(title).units.push(unit);
  }
  return groups;
}

function nameTokens(value) {
  return String(value || '')
    .replace(/['׳"]/g, '')
    .split(/[\s·]+/)
    .map((part) => part.trim())
    .filter((part) => part && !/^\d+$/.test(part));
}

function tokensMatch(a, b) {
  const x = String(a || '').toLowerCase();
  const y = String(b || '').toLowerCase();
  if (!x || !y) return false;
  if (x === y) return true;
  if (x.startsWith(y) || y.startsWith(x)) return true;
  const stripHe = (word) => word.replace(/^ה/, '');
  if (stripHe(x) === stripHe(y)) return true;
  if (
    x.length >= 3
    && y.length >= 3
    && x.slice(0, -1) === y.slice(0, -1)
    && 'הת'.includes(x.slice(-1))
    && 'הת'.includes(y.slice(-1))
  ) {
    return true;
  }
  if (x.length >= 3 && y.endsWith(x)) return true;
  if (y.length >= 3 && x.endsWith(y)) return true;
  return false;
}

/** Extra property word(s) to show above the short calendar label. */
export function unitCalendarPrefix(fullName, shortName) {
  const property = String(fullName || '').split('·')[0].trim();
  const leftover = nameTokens(property).filter(
    (word) => !nameTokens(shortName).some((short) => tokensMatch(word, short))
  );
  return leftover.join(' ');
}

/** Two calendar lines from the full inventory name: property / unit. */
export function unitCalendarLines(fullName) {
  const name = String(fullName || '').trim();
  if (!name) return { primary: '', secondary: '' };
  if (name.includes('·')) {
    const [left, ...rest] = name.split('·');
    return { primary: left.trim(), secondary: rest.join('·').trim() };
  }
  const words = name.split(/\s+/).filter(Boolean);
  if (words.length >= 2) {
    return { primary: words.slice(0, -1).join(' '), secondary: words[words.length - 1] };
  }
  return { primary: name, secondary: '' };
}

export function unitFullName(unitOrId, fallback = '') {
  if (unitOrId && typeof unitOrId === 'object') {
    return unitOrId.name || unitFullName(unitOrId.id, fallback);
  }
  const id = String(unitOrId || '');
  const hit = DEFAULT_RESORT_UNITS.find((unit) => unit.id === id);
  return hit?.name || fallback || id;
}

/** Guest-facing cabin name: phonetic in EN/AR, Hebrew source name otherwise. */
export function localizedUnitName(unitId, t, fallback = '') {
  const id = typeof unitId === 'object' ? unitId?.id : unitId;
  const hebrew = unitFullName(unitId, fallback);
  if (!id || typeof t !== 'function') return hebrew;
  return t(`${id}_name`, { defaultValue: hebrew });
}

export function unitMaxOccupancy(unitOrId, fallback = 6) {
  if (unitOrId && typeof unitOrId === 'object' && Number(unitOrId.max_occupancy) > 0) {
    return Math.max(1, Number(unitOrId.max_occupancy));
  }
  const id = typeof unitOrId === 'object' ? unitOrId?.id : unitOrId;
  const hit = DEFAULT_RESORT_UNITS.find((unit) => unit.id === id);
  return Math.max(1, Number(hit?.max_occupancy) || fallback);
}

/** Full inventory name for guest WhatsApp — never the calendar short label. */
export function whatsAppUnitName(unit, t) {
  const id = unit?.id || '';
  const fallback = unit?.name || unitFullName(id, '');
  if (id && typeof t === 'function') return t(`${id}_name`, { defaultValue: fallback });
  return fallback || id;
}

export function matchCanonicalUnitId(value) {
  const raw = String(value || '').trim();
  if (!raw) return null;
  if (NAME_TO_ID[raw]) return NAME_TO_ID[raw];
  const hit = DEFAULT_RESORT_UNITS.find((unit) => raw.includes(unit.name) || raw === unit.id);
  return hit?.id || null;
}

export function canonicalUnitSeed(tenantId, now = new Date().toISOString()) {
  return DEFAULT_RESORT_UNITS.map((unit) => ({
    id: unit.id,
    tenant_id: tenantId,
    name: unit.name,
    sort_order: unit.sort_order,
    unit_type: unit.unit_type || 'cabin',
    max_occupancy: unit.max_occupancy || 4,
    base_price_agorot: unit.base_price_agorot,
    cleaning_fee_agorot: 0,
    is_active: !isRetiredUnitId(unit.id),
    created_at: now,
    updated_at: now,
    version: 1
  }));
}

